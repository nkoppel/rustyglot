    -e: don't take an output file, instead, at the end of operations, write back into input file if the streams are of the same type. Otherwise, error.
    -out <file>

pgn options:
    -merge <pgn file>

    -min-elo <elo>
    -max-elo <elo>
    -min-high-elo <elo>
    -max-low-elo <elo>

    -min-time <time>
    -max-time <time>
    -min-increment <time>
    -max-increment <time>

    -no-draws
    -no-wins
    -no-white-wins
    -no-black-wins

bin options:
    -merge <file>

    -set-root <fen string>: default is startpos
    -filter <moves>
        format: e4 (d5/c5) d4 d6 / d4 d5

    -prune-weight <min weight>
    -prune-depth <max_depth>
    -prune-disconnected: remove all nodes not connected to the root node
    -keep-best <num best>?
    -keep-worst <num worst>?
    -white-only
    -black-only

    -clear-learning
    -uniform
